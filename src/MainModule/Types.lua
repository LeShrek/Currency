export type AddValue = {
    Name: string,
    DefaultValue: string | number| boolean,
    HideValue: boolean?
}

return {
    Default = {
        AddValue = {
            HideValue = false
        }
    }
}